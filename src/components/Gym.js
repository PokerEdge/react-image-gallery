import React from 'react';
import PictureContainer from '../PictureContainer';

const Gym = props => {

  return(
    <div className="photo-container">
      <PictureContainer title="" searchTerm={'gym'} />
    </div>
  );
}

export default Gym;
