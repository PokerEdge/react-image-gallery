import React from 'react';
import PictureContainer from '../PictureContainer';

const Coffee = props => {

  return(
    <div className="photo-container">
      <PictureContainer title="" searchTerm={'coffee'} />
    </div>
  );
}

export default Coffee;
